#include<stdio.h>

// 메인함수
int main()
{
    int a;
    a = 20;int i;


  int b = 2;
  
  b = b *2;
  if ( b > 200) return 0;
  printf("i = %d",i);
	main();
     printf("a=%d",a);  // 이 함수의 결과값은?
   
    return 0;
}